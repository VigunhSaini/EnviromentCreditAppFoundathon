{
    "name": "environmental-credit-contracts",
    "version": "1.0.0",
    "description": "Hardhat project for the EnvironmentalCredit ERC-721 contract",
    "scripts": {
        "compile": "hardhat compile",
        "test": "hardhat test",
        "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "hardhat": "^2.22.5"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.2"
    }
}